<%= simple_form_for(@bus_reservation) do |f| %>

  <%= f.input :date, html5: true %>
  <%= f.input :hour %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
